# EDR Console

EDR 管理控制台，基于 React + TypeScript + Vite 构建的现代化 Web 应用。

## 📋 功能模块

| 模块 | 说明 |
|------|------|
| 仪表盘 | 安全态势总览、关键指标展示 |
| 告警中心 | 告警列表、详情、处置 |
| 资产管理 | 终端列表、状态监控、分组管理 |
| 事件查询 | 事件搜索、过滤、详情 |
| 系统设置 | 策略配置、用户管理、系统配置 |

## 🏗️ 项目结构

```
console/
├── src/
│   ├── pages/           # 页面组件
│   ├── components/      # 通用组件
│   ├── stores/          # Zustand 状态管理
│   ├── api/             # API 请求封装
│   ├── hooks/           # 自定义 Hooks
│   ├── utils/           # 工具函数
│   ├── App.tsx          # 根组件
│   └── main.tsx         # 入口文件
├── public/              # 静态资源
├── tests/               # 测试文件
├── package.json
├── vite.config.ts
└── tsconfig.json
```

## 🔧 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.2 | UI 框架 |
| TypeScript | 5.3 | 类型系统 |
| Vite | 5.0 | 构建工具 |
| Ant Design | 5.12 | UI 组件库 |
| React Router | 6.21 | 路由 |
| React Query | 5.17 | 数据请求 |
| Zustand | 4.4 | 状态管理 |
| Axios | 1.6 | HTTP 客户端 |

## 🚀 开发指南

### 环境要求

- Node.js 18+ LTS
- pnpm 8+

### 安装依赖

```bash
pnpm install
```

### 启动开发服务器

```bash
pnpm dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
pnpm build
```

构建产物在 `dist/` 目录。

### 预览生产版本

```bash
pnpm preview
```

## 📝 代码规范

### Lint 检查

```bash
pnpm lint
```

### 自动修复

```bash
pnpm lint:fix
```

### 格式化代码

```bash
pnpm format
```

### 运行测试

```bash
pnpm test
```

## 📦 API 代理

开发环境下，`/api` 路径会代理到 `http://localhost:9000`（API Gateway）。

```typescript
// vite.config.ts
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:9000',
      changeOrigin: true,
    },
  },
}
```

## 🎨 主题定制

使用 Ant Design 的 ConfigProvider 进行主题定制：

```tsx
<ConfigProvider
  theme={{
    token: {
      colorPrimary: '#1890ff',
    },
  }}
>
  <App />
</ConfigProvider>
```

## 📄 目录说明

| 目录 | 说明 |
|------|------|
| `src/pages/` | 页面级组件，对应路由 |
| `src/components/` | 可复用的 UI 组件 |
| `src/stores/` | Zustand 状态 store |
| `src/api/` | API 请求封装和类型定义 |
| `src/hooks/` | 自定义 React Hooks |
| `src/utils/` | 工具函数 |
